import { LoadingState } from "@/components/loading-state";

const loading = () => {
  return (
    <LoadingState title="Loading Page" description="this may take a while" />
  );
};
export default loading;
